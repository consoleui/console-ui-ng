import { TemplateWrapperDirective } from './template-wrapper.directive';

describe('TemplateWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
