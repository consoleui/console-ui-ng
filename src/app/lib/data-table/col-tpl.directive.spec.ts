import { ColTplDirective } from './col-tpl.directive';

describe('ColTplDirective', () => {
  it('should create an instance', () => {
    const directive = new ColTplDirective();
    expect(directive).toBeTruthy();
  });
});
